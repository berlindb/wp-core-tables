{
    "name": "berlindb/wp-core-tables",
    "description": "Registers the WordPress core database tables as BerlinDB relations (schemas, queries, rows, relationships).",
    "type": "wordpress-plugin",
    "license": "MIT",
    "require": {
        "php": ">=8.1",
        "berlindb/core": "dev-master"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.6",
        "yoast/wp-test-utils": "^1.2",
        "phpstan/phpstan": "^2",
        "szepeviktor/phpstan-wordpress": "^2",
        "phpstan/extension-installer": "^1.4"
    },
    "repositories": {
        "berlindb-core": {
            "type": "vcs",
            "url": "https://github.com/berlindb/core"
        }
    },
    "autoload": {
        "psr-4": {
            "WPCoreTables\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "WPCoreTables\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "phpstan": "phpstan analyse --memory-limit=1G"
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "config": {
        "allow-plugins": {
            "phpstan/extension-installer": true
        }
    }
}
